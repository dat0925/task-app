# 応答言語について

すべての回答は日本語で行ってください。コード内のコメントや説明も日本語で書いてください。

---

## ◾️基本情報
- アプリ名称：Taskra-タスクラ
- リポジトリURL：https://github.com/dat0925/task-app
- 公開URL：https://app.taskra.jp
- DB：Supabase（MCP連携済み）
- 決済：Stripe（MCP連携済み）

---

## ◾️着手前にやること

### リポジトリ確認
- 最新のデータをgithubからcloneしてから取りかかって
- 他のAIや他の会話スレッドでも開発並行することがあるので、リポジトリに引き継ぎ書（ファイル名の例：HANDOVER.md）があれば確認して。なければ作って
  - AIも読みやすいし人間のエンジニアにも引き継ぎできるレベルで書いて
  - 特に「認証方式」「DB上の主要テーブルとRLSの状態」「決済まわりの構成（Webhook・Edge Function・料金プラン）」は必ず引き継ぎ書に明記して、次に読む人（AIも人間も）がゼロから調べ直さなくて済むようにして

### 現状確認
- 実装前に現在の動作を確認して、問題ないことを確認する
- 既存の仕様・制約と矛盾しないか確認する
- 今回の変更が認証・決済・個人情報に関わるものかどうかをまず判定して、該当する場合は下記「セキュリティチェック」を必ず実施する

---

## ◾️セキュリティチェック（該当する変更がある場合は必須）

### DB・テーブルまわり
- 新しいテーブルを作る/既存テーブルにuser_idなど個人を特定できるカラムを追加する場合、**CREATE TABLE文と同じマイグレーション内で必ず `ALTER TABLE ... ENABLE ROW LEVEL SECURITY;` とポリシーをセットで書く**
- 変更後、Supabase MCPで対象テーブルの `rls_enabled` を実際に確認する（口頭確認だけで済ませない）
- anonキーはブラウザに露出している前提で設計する（「フロントから直接呼ばない＝安全」ではない。RLSやEdge Function側での制御が本当の防御線）

### 決済（Stripe）まわり
- Webhookの署名検証（signature verification）が効いているか確認する
- service role keyやStripe secret keyはEdge Function等サーバー側のみで使用し、フロントエンドのコードやリポジトリに一切含めない
- 料金・プラン変更や返金トリガーなど「お金が動く」操作は、クライアント側から直接呼べないようにし、必ずサーバー側（Edge Function）で権限チェックしてから実行する設計になっているか確認する

### コード全般
- コミットする前に、diffにAPIキー・トークン・パスワードなどの秘匿情報が含まれていないか確認する
- .envやsecret類がリポジトリに含まれていないか（.gitignore対象になっているか）確認する
- 作業する上でPAT必要なら渡すので言うこと

---

## ◾️実装後にやること
- 変更後、実際に動作確認する
- 今回の変更が上記セキュリティチェックの対象だった場合、変更内容のサマリーで以下を明示する：
  - 新規/変更したテーブルとRLSの状態
  - 認証・決済フローに変更があったか
  - 触れなかった/触ってはいけないと判断した箇所
- HANDOVER.mdを更新する（変更点、次にやるべきこと、上記セキュリティ関連の状態を含めて）

---

## ◾️PUSH後にやること
- push後、コミットURLとアプリの公開URLを共有して
- push して終わりにせず、GitHub Pagesのビルドが成功するまで後追いで確認する
  - ビルドが失敗/停滞していたら、ログを確認して原因を報告する
  - 詰まっている場合は空コミットで再トリガーするなど対処し、成功を確認できるまで報告を続ける

---

## ◾️フロントエンドで致命的なエラーが起きた場合
- PC環境でChromeのデベロッパーコンソールを一緒に見ながら原因調査を進める運用とする。スマホ上の会話だけで無理に解決しようとせず、その旨を伝えて
