# Taskra 発信ストック（build in public）

X（@dat0925）に出す開発ログの**在庫と投稿ログ**。書き方の型は第2の脳の
「開発ログをXで発信する型（build in public）」に置いてある。ここは中身だけを貯める場所。

- **在庫**: これから出せるドラフト。上から順に使う。使ったら「投稿ログ」へ移す
- **リリースのたびに1本足す**（手順は `HANDOVER.md` の「【常設】リリースしたら X の投稿ドラフトを1本作る」）
- 投稿・予約は**本人が判断する**。AI はドラフトを置くところまで
- 日本語140字が上限（無料アカウント）。URLを付けるなら11.5字ぶん引く

最終更新: 2026-09-11（14を追加。既存の採番は変えていないので、予約済みの01〜03はそのまま）

---

## 在庫

### 14. 隠したことまで見せないと不具合になる（2026-09-11 / 決めた話）

```
「現在」フィルタが親タスクにしか効いていなかった。サブにも掛けると、全部隠れた親は展開しても何も出ない。押しても反応しないUIは壊れて見えるので「フィルタで2件を非表示」の一行を出した。隠すなら、隠したことまで見せないと不具合になる。
```

※ スクショ必須（サブタスクを展開して非表示件数の一行が出ている状態）。実データを写さない手順に従うこと。
※ 番号は採番順のID。在庫は上から順に使うので、これが次の候補。

### 00. 確認モーダルが背面に回り込んで押せない（2026-09-08 / 直した話）

```
「タスク完了の確認ダイアログが押せない」という報告。全画面表示と確認モーダルはどちらも画面全体に重ねる仕組みだが、確認モーダル側の重なり順（z-index）が全画面表示より低く、背面に回り込んで押せなくなっていた。数値を逆転させるだけの修正であんな挙動になる。
```

### 01. 押した回数の偶奇でDBが決まっていた（2026-09-07 / 直した話）✅ 予約済み 9/9(水)17:00

```
「完了をタップするとトーストは出るのに完了にならない」という報告。保存は成功していて、一覧を描き直す処理だけが飛ばされていた。しかも押すたびに完了と未完了がトグルするので、押した回数の偶奇でDBの状態が決まっていた。症状と原因が離れているバグはこわい。
```

補足: 一覧の再描画を戻すと入力中のコメントが消えるため避けていた。「ドロワを組み直さない」と「一覧も描かない」を取り違えていたのが真因。

### 02. 無料枠のEgressを3日で3.15GB（2026-09-01 / 数字の話）✅ 予約済み 9/11(金)20:00

```
個人開発のSupabase、無料枠5GBのEgressを3日で3.15GB使っていた。犯人は自分の実装。開きっぱなしのタブが2分ごとに全件フェッチしていて、24時間で68,000リクエスト。使っていたのは2人だけ。誰もいない深夜2時も日中と同じ量が出ていた。
```

### 03. 差分同期に直した話（2026-09-01 / 直した話）✅ 予約済み 9/15(火)8:00

```
全件取りに行く前に「件数と最終更新時刻だけ」を返す関数を1本置いた。前回と同じなら何もしない。非表示のタブでは同期もしない。1周期あたり10リクエスト・数百KBが、1リクエスト・数百バイトになった。無料枠は設計の甘さを可視化する装置だと思う。
```

※ 02 とセットで、同じ週に前後編として出すと流れが作れる。

### 04. 1000行で打ち切られていた（2026-06-23 / 直した話）

```
「タスクが消えた」と報告が来た。DBには残っている。原因はAPIが1リクエスト1000行で打ち切る仕様で、1321件持っている人の321件が最初から読み込まれていなかった。どれが落ちるかは順序依存なので、ランダムに消えたように見える。ページングを入れて全部戻った。
```

### 05. 握りつぶしたnullがデータを消す（2026-08-01 / 直した話）

```
いちばん肝が冷えたバグ。タスクの中身だけが空で上書きされていた。自動保存が400ms後に走るとき、画面をもう閉じていると要素がすべてnullになり、空文字のフォールバックがそのまま保存されていた。握りつぶしたnullは、いつか本番でデータを消す。
```

### 06. 1タップが2回判定になる（2026-09-06 / 直した話）

```
「+1日を1回押すと2日進む。指を弾くように離したときだけ正しい」という報告。タップを速く見せるために合成クリックを出していて、そのあとブラウザが出す本物のクリックと二重に走っていた。他のボタンは350msのデバウンスが偶然隠していただけだった。
```

### 07. 更新ボタンを常設しなかった理由（2026-08-30 / 決めた話）

```
「スマホのアプリを新しくする方法がない」と言われて直した。今までは新版を見つけると予告なくリロードしていて、入力が消えるのが怖くて実質使えない機能だった。ヘッダーに更新ボタンを常設する案は却下。99%の時間いらないものを、画面の一等地に置きたくない。
```

### 08. アイコンか、文字か（2026-08-28 / 決めた話）

```
スマホの下に「コピー」「最上部」を足すとき、アイコンだけ・文字だけ・両方で迷った。↑だけだと「ひとつ上へ動かす」に見える。結局アイコンを一段小さくして文字を主役にした。色を付けるのは「完了」だけ。3つ並べて全部目立たせると、主役が消える。
```

※ スクショ必須（下部アクションバー）。

### 09. 自分の使い方から出た地味な機能（2026-08-08 / 作った話）

```
メモにWindowsのフォルダパスを貼ることが多い。スマホでは開けないけれど、PCに戻ったとき毎回コピーしたい。だからタップでコピーだけする挙動にした。自分の使い方から出てくる機能はこういう地味なやつばかりで、たぶんそれでいい。
```

### 10. 有料の線をどこに引くか（2026-08-09 / 決めた話）

```
画像とファイルの添付を付けて、有料プラン限定にした。個人開発で有料の線をどこに引くかは迷ったけど、機能の優劣ではなく「自分の持ち出しが増えるところ」に引くのがいちばん説明しやすい。ストレージ代は使う人の分だけ増えるので。
```

### 11. 自分からも読めないメモ（2026-08-02 / 作った話）

```
パスワードや鍵をメモしたい。でも自分のDBに平文で置きたくない。合言葉で解錠するメモを作って、運営（＝自分）からも読めないようにした。個人開発でここまでやるか迷ったけど、自分が安心して使えないものを人に勧めるのは無理だと思った。
```

### 12. 自分のアプリに無料でプレミアムになれる穴があった（2026-08-09 / 直した話）

```
決済まわりを監査したら、自分のアプリに無料でプレミアムになれる穴があった。行がまだ無い新規ユーザーなら、プラン名を書いた行を直接作れてしまう。防御をRLS一本に頼っていたのが原因。freeプラン以外のINSERTを弾いて塞いだ。実際に再現してから直した。
```

※ 決済の穴を出すのは、**塞ぎ終わってから**。手口の詳細は書かない。

### 13. 引き継ぎ書の一文でビルドが落ちた（2026-08-22 / 直した話）

```
引き継ぎ書に書いたテンプレート記法のせいで、GitHub Pagesのビルドが落ちていた。本番は旧版が配信され続けるので、画面上は何も起きていないように見える。しかも原因になったのは「この記法は0件です」と書いた文章そのものだった。
```

---

## 投稿ログ

投稿したら在庫から移して1行足す。日付・URL・反応（表示数／いいね）を残す。

| 予定/投稿日 | 見出し | 状態 | URL | 反応 |
|---|---|---|---|---|
| 2026-09-09(水) 17:00 | 01 押した回数の偶奇でDBが決まっていた | 予約済み | | |
| 2026-09-11(金) 20:00 | 02 無料枠のEgressを3日で3.15GB | 予約済み | | |
| 2026-09-15(火) 8:00 | 03 差分同期に直した話 | 予約済み | | |

予約の確認・編集は https://x.com/compose/post/unsent/scheduled 。
投稿されたらこの表に URL と反応（表示数／いいね）を書き足す。
